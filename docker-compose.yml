services:
  codebridge:
    image: code-bridge:latest
    environment:
      - PORT=8501
    ports:
      - "80:8501"     # ← 80으로 변경 (기존 "8501:8501" 제거)
    volumes:
      - ./data:/app/data:rw
      - ./images:/app/images:rw
    restart: unless-stopped
